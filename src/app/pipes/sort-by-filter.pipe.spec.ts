import { SortByFilterPipe } from './sort-by-filter.pipe';

describe('SortByFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
